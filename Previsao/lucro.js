const bitcoinValue = require('./bitcoinValue')
const realValue = require('./realValue')


const lucro = function(btc, brl, porcento){
    // Porcento ser colocado a porcentagem dividida por 100
    // bitcoinValue''
    return console.log("Real " + brl)
}

module.exports = lucro