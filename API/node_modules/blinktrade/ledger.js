import { BlinkTradeWS } from './src'

const blinktrade = new BlinkTradeWS()

blinktrade.connect().then(() => {
  return blinktrade.login({ username: 'cesaraugusto', password: 'abc12345' })
}).then(() => {
  return blinktrade.requestLedger()
  // return blinktrade.requestLedger({}, (err, ledger) => {
    // console.log(ledger)
  // })
}).then(data => {
  console.log(data)
}).catch(console.log)
